import type { ApiReferenceConfiguration } from '@scalar/types/api-reference'

export const config: ApiReferenceConfiguration = {
  url: 'https://registry.scalar.com/@scalar/apis/galaxy?format=json',
}
