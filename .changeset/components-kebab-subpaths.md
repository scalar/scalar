---
'@scalar/components': minor
---

refactor: rename component subpath exports to kebab-case without the `Scalar` prefix (e.g. `@scalar/components/scalar-button` is now `@scalar/components/button`). The exported component names are unchanged (`ScalarButton`, etc.).
