---
'@scalar/api-reference': patch
---

Only show the "one of" hint above required OAuth scopes when there is more than one scope to choose between. A single scope is now listed plainly, even when a scope-free alternative (like an API key) also satisfies auth.
