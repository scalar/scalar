---
'@scalar/components': patch
---

chore: add `./helpers` and individual component subpath exports so consumers can import directly from component subpaths instead of the package barrel, improving tree-shaking
