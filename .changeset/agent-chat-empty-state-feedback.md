---
'@scalar/agent-chat': patch
---

Rework the empty state to the shared pattern: the logo and large heading are replaced by a bottom-docked "How can I help you today?" heading with example prompts in a list that prefill the composer, and the add-API affordances (the plus button popovers and the "Load additional APIs" row) are temporarily disabled
