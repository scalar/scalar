---
'@scalar/api-client': minor
'@scalar/workspace-store': minor
'@scalar/types': minor
'@scalar/mock-server': minor
---

Support OpenAPI 3.2 OAuth device authorization with verification codes, cancellable token polling, stored credentials, and OAuth metadata discovery. Add mock device authorization and approval endpoints with pending, denial, expiry, and polling backoff responses.

Use consistent form-encoded Basic credentials and environment substitution across OAuth token and refresh flows. Allow loopback HTTP metadata, coerce discovery fields consistently, and report device-code expiry clearly.
