---
'@scalar/asyncapi-validator': minor
---

Add `@scalar/asyncapi-validator`, a package that validates AsyncAPI documents (2.x and 3.x) against the AsyncAPI Specification. It is built on `@scalar/json-schema-validator` and uses the official schemas from `@asyncapi/specs`.
