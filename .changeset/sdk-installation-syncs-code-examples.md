---
'@scalar/api-reference': patch
'@scalar/api-client': patch
---

Sync the SDK installation tabs with the operation code examples: picking a language under "Client Libraries" now switches every operation's code sample to that language's custom example
