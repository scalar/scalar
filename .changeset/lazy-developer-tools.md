---
'@scalar/api-reference': patch
---

Load the developer toolbar only when it is visible. Configurations with `showDeveloperTools: 'never'` and production pages using the default setting no longer load its configuration, share, and deploy UI.
