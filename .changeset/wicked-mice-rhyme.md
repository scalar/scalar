---
'@scalar/openapi-to-markdown': patch
---

Reject unsupported `connect` operation selectors so markdown generation fails with a clear error instead of silently producing an empty operations section.
