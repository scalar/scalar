---
'@scalar/json-schema-validator': minor
---

Add `@scalar/json-schema-validator`, a standalone engine that validates documents against a JSON Schema with Ajv and returns human-friendly errors. It is the shared core that `@scalar/openapi-validator` now builds on (and that a future AsyncAPI validator can reuse).
