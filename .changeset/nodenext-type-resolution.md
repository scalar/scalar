---
'@scalar/openapi-types': patch
---

fix: add explicit `.js` file extensions to relative imports in the published `2.0`, `3.0`, `3.1` and `3.2` type declarations, so they resolve with `moduleResolution: nodenext` and `node16`
