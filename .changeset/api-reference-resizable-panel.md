---
'@scalar/api-reference': minor
'@scalar/types': patch
---

Make the agent chat panel resizable: a drag handle on the panel's left edge resizes the width (persisted, clamped between 360px and 75% of the viewport, double-click to reset), following the docs editor's shared resizable pane mechanics, with keyboard resizing and ARIA value upkeep on the handle
