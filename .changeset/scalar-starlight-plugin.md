---
'@scalar/starlight': minor
---

Add `@scalar/starlight`, a Starlight plugin that renders a Scalar API reference. It injects a route and adds a sidebar entry, so you no longer have to hand-create a page and embed the component yourself.
