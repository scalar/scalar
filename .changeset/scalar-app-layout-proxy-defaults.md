---
'scalar-app': patch
---

fix: apply layout-aware CORS proxy defaults when creating workspaces and loading imported documents, so the web client uses proxy.scalar.com and the desktop client skips the proxy unless configured in settings.
