---
'@scalar/workspace-store': patch
'@scalar/api-client': patch
---

Add a "generate example from schema" button to the request body, so the schema-generated fields stay reachable once an operation ships custom examples
