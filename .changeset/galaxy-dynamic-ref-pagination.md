---
'@scalar/galaxy': patch
---

Express the `/planets` pagination with a generic `Paginated` template that binds its item type through a JSON Schema 2020-12 `$dynamicRef` / `$dynamicAnchor`, demonstrating the `Paginated<T>` pattern
