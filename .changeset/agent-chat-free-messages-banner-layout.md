---
'@scalar/agent-chat': patch
---

Fix the free-messages upsell banner layout at narrow widths: the copy now wraps as one flowing sentence and the "Read more" button stays on a single line instead of every element fragmenting onto its own row.
