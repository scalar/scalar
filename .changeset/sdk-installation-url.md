---
'@scalar/api-reference': minor
'@scalar/workspace-store': minor
'@scalar/schemas': minor
---

Add an optional `url` to `x-scalar-sdk-installation` entries that links out to the package or repository (for example on GitHub, npm or PyPI). The Client Libraries card renders it as a "View on …" link with a friendly label for well-known hosts
