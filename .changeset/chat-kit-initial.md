---
'@scalar/chat': patch
---

Add the chat kit foundation: ChatRoot (the density axis and injectable copy dictionary), the session core ported from the converged org implementations (Map of Chat instances, IndexedDB history with migrate-on-read that keeps existing database names, the two-watcher persistence pair, lazy restore), the dual-mode approval store, useChatError, and ChatViewport implementing the hardened reserved-min-height scroll anchor
