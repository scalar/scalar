---
'@scalar/types': patch
---

Add the agent panel configuration and translation shape to the API Reference types: the resizable panel options plus the agent panel header and MCP menu translation keys (`agent.closeChat`, `agent.expand`, `agent.collapse`, `mcp.installCursor`, `mcp.installVsCode`, `mcp.copyUrl`).
