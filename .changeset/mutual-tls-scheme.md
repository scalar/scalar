---
'@scalar/workspace-store': patch
'@scalar/types': patch
'@scalar/api-client': patch
---

Keep the `mutualTLS` security scheme type instead of turning it into an apiKey form, and show read-only authentication guidance for mutual TLS and unsupported browser broker credentials.
