---
'@scalar/scalar': patch
---

Add the new `@scalar/scalar` package: the Scalar API Reference bundled as a single, code-split ES module, published under a short name so it can be loaded straight from a CDN with `import { createApiReference } from 'https://cdn.jsdelivr.net/npm/@scalar/scalar'`.
