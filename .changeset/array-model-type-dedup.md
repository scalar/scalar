---
'@scalar/api-reference': patch
---

Stop showing a redundant `array object[]` next to the model link for arrays of named objects. The type now reads `array · Resource[]` instead of `array object[] · Resource[]`
