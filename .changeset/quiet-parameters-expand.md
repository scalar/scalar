---
"@scalar/api-reference": minor
"@scalar/schemas": minor
"@scalar/types": minor
---

Add `expandAllParameters`, defaulting to `true`. Set it to `false` to start operation parameters collapsed and expand each parameter on click.
