---
'@scalar/api-reference': patch
'@scalar/workspace-store': patch
---

Use non-reactive workspace stores during server rendering to reduce document read overhead, while preserving configured servers, environment defaults, and reactive browser hydration.
