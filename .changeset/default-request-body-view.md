---
'@scalar/workspace-store': minor
'@scalar/api-client': minor
---

Add `x-scalar-default-request-body-view` document extension to open the request body editor in the Form view by default (set it to `form`, or leave it as `raw`)
