---
'@scalar/workspace-store': patch
---

Preserve authored operation order when preparing server-store path items and retain the original OpenAPI version through resolved rendering and sparse browser loading, so prepared server renders keep navigation order and the version badge.
