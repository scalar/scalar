---
'@scalar/openapi-validator': minor
'@scalar/openapi-parser': patch
'@scalar/types': patch
---

Add a new `@scalar/openapi-validator` package that validates OpenAPI documents on its own. The parser keeps working exactly as before, it now uses this package under the hood.
