---
'@scalar/helpers': minor
---

feat(helpers): add a small dependency-free `compareVersions` helper (and `isVersionLessThanOrEqual` convenience wrapper) under `general/compare-versions` for ordering semver-style version strings.
