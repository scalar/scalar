---
'@scalar/mock-server': patch
---

Serve OAuth2 authorization server metadata at the declared oauth2MetadataUrl, advertising local mock endpoints and the configured grants and scopes.

Normalize absolute OAuth token URLs to route paths when registering mock authentication routes.
