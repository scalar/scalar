---
"scalar-fastapi": patch
"scalar-ninja": patch
---

Escape JavaScript and favicon URL attributes to prevent values from injecting HTML.

Apply the same attribute escaping to Django Ninja JavaScript and favicon URLs.
