{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint:fix": {},
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "types:check": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
