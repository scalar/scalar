{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint:fix": {},
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "types:check": {}
  }
}
