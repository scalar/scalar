{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint:fix": {},
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "types:check": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
