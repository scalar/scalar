{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter api-client-web dev",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "publish": "pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "@babel/runtime": "^7.22.10",
    "lerna": "^7.1.5",
    "vitest": "^0.34.1"
  }
}
