/**
 * Parameter Scalar extensions.
 *
 * Applied on OpenAPI parameters (path, query, header, cookie). Used by the API client for global
 * parameter injection and example state.
 */
export { XGlobal } from './x-global'
