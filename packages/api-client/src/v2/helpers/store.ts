/**
 * Set as a string so that different instances of api-client can reference inject the same store
 *
 * Do not use an actual symbol for this
 */
export const WORKSPACE_SYMBOL = 'WORKSPACE_SYMBOL' as const
