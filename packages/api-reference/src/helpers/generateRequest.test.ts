import { generateRequest } from 'src/helpers/generateRequest'
import { describe, expect, it } from 'vitest'

describe.skip('generateRequest', () => {
  it('sets example values', () => {
    // expect(generateRequest({}, {}, 'https://example.com')).toMatchObject({
    //   id: 10,
    // })
  })
})
