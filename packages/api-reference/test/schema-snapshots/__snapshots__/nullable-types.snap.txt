# Nullable types
# OpenAPI 3.1 uses a type array; 3.0 used `nullable: true`. Both reach the
# renderer, so both are pinned here.

- legacyNullable — string · nullable
- multiUnion — string | number | boolean
- nullableArray — array string[] | null
- unionNull — string | null
