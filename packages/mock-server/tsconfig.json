{
  "extends": "../../tsconfig.json",
  "include": ["src", "test", "esbuild.ts", "env.d.ts"],
  "compilerOptions": {
    // hono/streaming exposes Node stream types in its declarations.
    "types": ["node"],
    "paths": {
      "@/*": ["./src/*"],
      "@test/*": ["./test/*"]
    }
  }
}
