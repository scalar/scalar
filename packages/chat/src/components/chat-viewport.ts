/**
 * The data attribute consumers set on each user message element; the last
 * one is the anchor `ChatViewport` pins to the top of the viewport. Provided
 * as a constant so the anchor is never located through a class query.
 */
export const CHAT_VIEWPORT_ANCHOR_ATTRIBUTE = 'data-chat-viewport-anchor'
