openapi: 3.0.0
info:
  title: Invalid pattern
  version: 1.0.0
paths: {}
components:
  schemas:
    test:
      type: string
      # lookbehind patterns validate ok in Node >= 8.10.0
      pattern: '['
