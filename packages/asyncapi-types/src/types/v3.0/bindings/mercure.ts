/**
 * Protocol-specific information for a Mercure server.
 * This object MUST NOT contain any properties. Its name is reserved for future use.
 */
export type MercureServerBinding = Record<string, never>

/**
 * Protocol-specific information for a Mercure channel.
 * This object MUST NOT contain any properties. Its name is reserved for future use.
 */
export type MercureChannelBinding = Record<string, never>
