// Generated by scripts/generate-openapi-parser.ts — do not edit by hand.
// Regenerate with `bun run codegen`.
// @ts-nocheck

import { isObject } from './_helpers/is-object'
import {
  type SpecificationExtensions,
  parseSpecificationExtensions,
  validateSpecificationExtensionsShape,
} from './specification-extensions'

/**
 * ServerVariable
 *
 * https://spec.openapis.org/oas/v3.1.0#server-variable-object
 */
export type ServerVariable = {
  enum?: string[]
  default: string
  description?: string
} & SpecificationExtensions

export const validateServerVariableShape = (input: unknown): boolean => {
  if (!isObject(input)) return false
  return (
    (input.enum === undefined || (Array.isArray(input.enum) && input.enum.length >= 1)) &&
    typeof input.default === 'string' &&
    (input.description === undefined || typeof input.description === 'string')
  )
}

export const parseServerVariable = (input: unknown): ServerVariable => {
  if (!isObject(input))
    return {
      default: '',
    }
  const _enum = input.enum
  const _default = input.default
  const _description = input.description
  if (
    (_enum === undefined || (Array.isArray(_enum) && _enum.length >= 1)) &&
    typeof _default === 'string' &&
    (_description === undefined || typeof _description === 'string')
  )
    return { ...input } as ServerVariable
  return {
    ...input,
    ...(_enum !== undefined && { enum: Array.isArray(_enum) && _enum.length >= 1 ? _enum : [] }),
    default: typeof _default === 'string' ? _default : _default !== undefined ? String(_default) : '',
    ...(_description !== undefined && {
      description: typeof _description === 'string' ? _description : String(_description),
    }),
  } as unknown as ServerVariable
}
